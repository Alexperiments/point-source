# Frontend

React + Vite frontend for the single-app migration.

## API defaults

- `VITE_API_BASE_URL=""` (same-origin)
- `VITE_AUTH_BASE_PATH="/v1/auth"`
- `VITE_CHAT_STREAM_PATH="/v1/llm/chat/stream"`
